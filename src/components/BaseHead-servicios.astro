---
import '../style/servicios.css';
---

<div  class="BaseHead-servicios">
  <h1>SERVICIOS</h1>
    <div class="barra">
        <a href="/herramientas">Herramientas</a>
        <a href="/servicios">Servicios</a>
    </div>
</div>


<script>
document.addEventListener('DOMContentLoaded', () => {
  const links = document.querySelectorAll('.BaseHead-servicios .barra a');
  const currentPath = window.location.pathname; // ruta actual de la página

  links.forEach(link => {
    const href = link.getAttribute('href');

    // Comparar ruta del link con la ruta actual
    if (href === currentPath) {
      link.classList.add('active');
    }
  });
});
</script>